# Employees implementation notes

The first business slice will add Employee and Department persistence in the `employees` PostgreSQL schema, REST contracts and the employee list/organization structure UI. Unknown workflow rules from the product specification remain open rather than being guessed.
